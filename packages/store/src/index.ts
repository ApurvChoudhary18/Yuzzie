/**
 * @yuzie/store — Local SQLite cache and offline outbox for the Yuzie CLI.
 *
 * Session 0 placeholder. Product logic arrives in a later session; see SPEC.md §18.
 */
export const PACKAGE_NAME = '@yuzie/store' as const
